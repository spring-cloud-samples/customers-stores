spring:
  application:
    name: customersui
  cloud:
    config:
     uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:${CONFIG_SERVER_URI:http://localhost:8888}}
#     failFast: true
      